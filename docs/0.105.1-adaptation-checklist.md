# 0.105.1 适配清单

## 已完成

- `种子分析` 页面已补版本选择，并复用全局 `SelectedGameVersion`
- 游戏版本列表已改为扫描 `data/<version>` 目录自动发现
- `NeowGenerator` 的 modern rules 判定已从“仅 0.103.2”改为“0.103.2 及之后版本”

## 已确认的本地源码入口

- 本地源码目录：
  - `Slay the Spire 2 版本0.105.1源码（游戏源码，读取用）`
- 关键模型目录：
  - `src/Core/Models/Acts`
  - `src/Core/Models/Cards`
  - `src/Core/Models/Events`
  - `src/Core/Models/Relics`
  - `src/Core/Models/RelicPools`
  - `src/Core/Models/PotionPools`

## 已确认的 0.103.2 -> 0.105.1 差异

- 章节模型文件名未变
- 遗物池模型文件名未变
- 事件模型文件名未变
- 卡牌模型新增 1 张：
  - `Wither`
- 遗物模型新增 3 个：
  - `FishingRod`
  - `Kaleidoscope`
  - `SilkenTress`

## 待完成

### 1. 数据导出

- 导出 `data/0.105.1/neow/options.json`
- 导出 `data/0.105.1/ancients/options.json`
- 导出 `data/0.105.1/ancients/options.zhs.json`
- 导出 `data/0.105.1/sts2/acts.json`
- 导出 `data/0.105.1/sts2/localization/zhs/*.json`

### 2. Neow / 开局规则核对

- 核对 0.105.1 的正面池 / 负面池候选
- 核对互斥规则是否和 0.103.2 一致
- 核对是否存在新的 `NextBool()` 分支或多人限制变化

### 3. 池与目录核对

- 卡牌池是否包含 `Wither`
- 遗物池是否包含 `FishingRod / Kaleidoscope / SilkenTress`
- 商店池 / 事件池 / 先古池分类是否变化
- 药水池是否有新增 / 稀有度调整

### 4. 分析功能回归

- 配置页 roll 种
- 种子分析
- 事件概率预览
- 遗物概率预览
- 海玻璃预览
- 精确路线分析
- 商店筛选

### 5. 结果一致性验证

- CLI 与 UI 一致
- 同版本下配置页与种子分析一致
- 0.105.1 不混读 0.103.2 数据
